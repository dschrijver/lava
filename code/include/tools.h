#ifndef TOOLS_H
#define TOOLS_H


/**
 * @brief Compute the minimum value of a macroscopic quantity.
 */
double calculate_minimum_macroscopic_quantity(double *macroscopic_quantity);

/**
 * @brief Compute the maximum value of a macroscopic quantity.
 */
double calculate_maximum_macroscopic_quantity(double *macroscopic_quantity);

#endif